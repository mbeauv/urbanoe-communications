// @flow

/**
 * List of properties and their associated values for a given issue.
 */
export type IssueProperties = {
  +size?: string,
  +hazardLevel?: string,
  +propertyType?: string,
  +petType?: string,
  +graffityQuality?: string,
}
